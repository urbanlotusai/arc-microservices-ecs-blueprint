# =============================================================================
# 02-network - PCI DSS Compliance Profile
# =============================================================================
# No profile-specific overrides — VPC sizing (10.0.0.0/16) is identical across
# all profiles. PCI DSS Req 1 network-segmentation controls are enforced
# downstream by 03-security-group and subnet placement, not by this module's
# inputs.
# =============================================================================
