# =============================================================================
# 02-network - HIPAA Compliance Profile
# =============================================================================
# No profile-specific overrides — VPC sizing (10.0.0.0/16) is identical across
# all profiles. Network isolation for PHI workloads is enforced downstream by
# 03-security-group and subnet placement, not by this module's inputs.
# =============================================================================
